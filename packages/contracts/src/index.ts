/**
 * @slk/contracts — the Inventory API's source of truth.
 *
 * The OpenAPI document generated from here is what `slk-mobile` builds its
 * Dart client from in CI. That is the point of the package: a breaking API
 * change should fail the mobile build, not surface as a runtime error on a
 * warehouse floor.
 *
 * Never hand-edit a generated client to work around something here. Fix it
 * here and regenerate.
 */

export {};
